################################################################################
#
# Shape constraint matrix method:
# Strata
#
################################################################################

#' @export
shapeConstr.strata <- function(x, shape, intercept = FALSE, ...){

  # Call the default method
  shapeConstr.default(x, shape = shape, intercept = intercept, ...)

}
